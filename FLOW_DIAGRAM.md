# 🔄 Diagrama de Fluxo de Dados - Arena Neon FS

## 1. INICIALIZAÇÃO DO APP

```
┌─────────────────────────────────────────────────────────┐
│                    App.tsx inicia                       │
└─────────────────────────────────────────────────────────┘
                         ↓
        ┌───────────────────────────────────┐
        │  useAuth Hook                     │
        │  - Escuta Firebase Auth           │
        │  - Detecta login/logout           │
        │  - Carrega dados do usuário       │
        └───────────────────────────────────┘
                         ↓
        ┌───────────────────────────────────┐
        │  currentUser setado?               │
        └───────────────────────────────────┘
            ↓ SIM                 ↓ NÃO
         Logado              Tela de Login
            ↓                     ↓
    Carregar Save         Mostrar Auth Form
         ↓                       ↓
    useGameState          Aguardando Input
         ↓                       ↓
    Iniciar Jogo           [usuario_login]
                                 ↓
                            Volta ao SIM
```

---

## 2. FLUXO DE AUTENTICAÇÃO

```
┌──────────────────────────────────────────────────────────┐
│                 Tela de Autenticação                     │
│  [Email/User] [Senha] [Registrar/Entrar]                │
└──────────────────────────────────────────────────────────┘
                         ↓
            handleAuthAction() chamado
                         ↓
        ┌─────────────────────────────────┐
        │ Validar inputs                  │
        │ - Min 3 chars username          │
        │ - Min 6 chars password          │
        │ - Email válido                  │
        └─────────────────────────────────┘
                         ↓
            ┌────────────────────────┐
            │ Register ou Login?     │
            └────────────────────────┘
               ↓                  ↓
        registerWithEmail()  loginWithEmailOrUsername()
               ↓                  ↓
        Firebase Auth        Firebase Auth
               ↓                  ↓
        Criar User              Check Email
        + Username              + Password
               ↓                  ↓
            Sucesso → loadGameFromCloud()
               ↓
        setCurrentUser(user)
               ↓
        authMode = "hidden"
               ↓
        Exibir Menu Principal
```

---

## 3. FLUXO DO JOGO

```
┌──────────────────────────────────┐
│  Clique em "INICIAR MISSÃO"      │
└──────────────────────────────────┘
                ↓
    ┌─────────────────────────────┐
    │ Preparar Jogo               │
    │ - Reset gameState           │
    │ - Initialize stats          │
    │ - Start game loop           │
    │ - Load daily reward         │
    └─────────────────────────────┘
                ↓
    ┌─────────────────────────────┐
    │ useGameLoop Hook            │
    │ requestAnimationFrame()     │
    │ Loop a 60 FPS               │
    └─────────────────────────────┘
                ↓
    ┌─────────────────────────────┐
    │ A cada frame:               │
    │ - Spawn inimigos            │
    │ - Atualizar posições        │
    │ - Detectar colisões         │
    │ - Calcular damage           │
    │ - Atualizar UI              │
    └─────────────────────────────┘
                ↓
        Jogador morre?
        SIM ↓
        ┌─────────────────────────────┐
        │ Game Over                   │
        │ - Calcular score            │
        │ - Atualizar high score      │
        │ - Salvar stats              │
        │ - Update leaderboard        │
        └─────────────────────────────┘
            ↓
        handleGameOver()
            ↓
        updateGlobalStats()
            ↓
        updateLeaderboard()
            ↓
        Volta ao Menu
```

---

## 4. PERSISTÊNCIA DE DADOS

```
┌────────────────────────────────────────────────────────┐
│         APP STATE (React)                              │
│  - gameState                                           │
│  - upgrades                                            │
│  - globalStats                                         │
│  - leaderboard                                         │
└────────────────────────────────────────────────────────┘
        ↓                               ↓
   (Contínuo)                    (Ao sair do jogo)
        ↓                               ↓
   LOCAL STORAGE                  FIREBASE CLOUD
   (localStorage)                 (Firestore)
        ↓                               ↓
  ┌──────────────┐            ┌──────────────────┐
  │ Rápido       │            │ Sincronizado     │
  │ Local        │            │ Multi-device     │
  │ Browser      │            │ Backup           │
  └──────────────┘            └──────────────────┘
```

---

## 5. CARREGAMENTO DO LEADERBOARD

```
┌─────────────────────────────────┐
│  Clique em "RANKING"            │
└─────────────────────────────────┘
           ↓
   setShowLeaderboard(true)
           ↓
   useLeaderboard Hook ativa
           ↓
   ┌─────────────────────────────┐
   │ loadLeaderboardData()        │
   │ setIsLoading(true)           │
   └─────────────────────────────┘
           ↓
   Firebase Query:
   collection("leaderboard")
   orderBy("highScore", desc)
   limit(50)
           ↓
   Retorna array de jogadores
           ↓
   ┌─────────────────────────────┐
   │ Validar dados               │
   │ - Filtrar entries inválidas │
   │ - Ordenar por score         │
   │ - Formatar playerName       │
   └─────────────────────────────┘
           ↓
   setLeaderboard(validData)
           ↓
   setIsLoading(false)
           ↓
   ┌─────────────────────────────┐
   │ Renderizar Leaderboard      │
   │ - Mostrar top 50            │
   │ - Rank + Medal emoji        │
   │ - Score formatado           │
   │ - Prestige badge            │
   └─────────────────────────────┘
           ↓
   Polling: setInterval(30s)
           ↓
   Atualiza leaderboard a cada 30s
```

---

## 6. AUTO-SAVE (60s)

```
┌─────────────────────────────────┐
│  Jogo em andamento              │
│  (gameState.isGameStarted)      │
└─────────────────────────────────┘
           ↓
   setInterval(60000ms)
           ↓
   ┌─────────────────────────────┐
   │ handleCloudSave()           │
   │ setCloudStatus("saving")    │
   └─────────────────────────────┘
           ↓
   saveGameToCloud(
     currentUser,
     gameState,
     upgrades,
     globalStats
   )
           ↓
   Firebase writes to /users/{uid}
   document with merge:true
           ↓
   ┌─────────────────────────────┐
   │ Sucesso?                    │
   └─────────────────────────────┘
      SIM ↓           NÃO ↓
      setCloudStatus   setCloudStatus
      ("saved")        ("error")
           ↓               ↓
      ✅ Badge verde   ❌ Badge vermelho
           ↓
      Após 3s
           ↓
      setCloudStatus("idle")
```

---

## 7. CICLO DE VIDA DE UM HOOK

```
┌─────────────────────────────────────────────┐
│  Component Monta                            │
└─────────────────────────────────────────────┘
                    ↓
         useEffect() executa
                    ↓
    ┌─────────────────────────────┐
    │ Initialize State            │
    │ - useState setups           │
    │ - Setup listeners           │
    │ - Load data                 │
    └─────────────────────────────┘
                    ↓
    ┌─────────────────────────────┐
    │ Monitor Dependencies        │
    │ - useEffect([deps])         │
    │ - Re-run se deps mudarem    │
    └─────────────────────────────┘
                    ↓
    ┌─────────────────────────────┐
    │ Component Unmounts          │
    │ - Cleanup: clearInterval()  │
    │ - Cleanup: unsubscribe()    │
    │ - Cleanup: removeListener() │
    └─────────────────────────────┘
                    ↓
           Hook destruído
```

---

## 8. ESTRUTURA DE PASTA SIMPLIFICADA

```
src/
 ├─ hooks/                    ← Custom logic
 │   ├─ useAuth.ts          → User authentication
 │   ├─ useGameState.ts     → Game persistence
 │   ├─ useLeaderboard.ts   → Ranking data
 │   └─ index.ts            → Centralized exports
 │
 ├─ services/                ← External integrations
 │   ├─ firebase.ts         → Database & Auth
 │   ├─ audio.ts            → Sound system
 │   └─ competitive.ts      → Game calculations
 │
 ├─ types/                  ← TypeScript definitions
 │   ├─ game.types.ts       → Game interfaces
 │   └─ index.ts            → Centralized exports
 │
 ├─ constants/              ← Hard-coded values
 │   ├─ game.constants.ts   → Game settings
 │   ├─ upgrades.constants.ts → Upgrade data
 │   └─ index.ts            → Centralized exports
 │
 ├─ components/             ← React components
 │   ├─ GameCanvas.tsx      → Game rendering
 │   ├─ Leaderboard.tsx     → Ranking display
 │   └─ ...
 │
 └─ App.tsx                 ← Main entry point
```

---

## 📊 Estado Global vs Local

```
GLOBAL (App.tsx)          LOCAL (Component)
├─ currentUser            ├─ formInputs
├─ gameState              ├─ modalOpen
├─ upgrades               ├─ dropdownActive
├─ globalStats            ├─ tooltipText
├─ leaderboard            └─ animationState
├─ highScore
└─ cloudStatus
```

---

**Gerado**: 1º de dezembro de 2025
